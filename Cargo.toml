[package]
name = "edge-monitor"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"] #essential for WASM

[dependencies]
wasm-bidgen = "0.2"
web-sys = { version = "0.3", features = ["Window", "Request", "Response"] }
